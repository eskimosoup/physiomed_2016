<%= inline_editable @page %>

<% if @what_we_dos %>
  <%= render(
     partial: 'pages/what_we_do',
     object: @page,
     as: :page,
     locals: {
       what_we_dos: @what_we_dos
     }
  ) %>
<% else %>
  <%= render @page.style %>
<% end %>
